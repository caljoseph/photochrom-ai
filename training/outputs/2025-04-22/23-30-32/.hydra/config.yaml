data:
  root_dir: /path/to/data
  image_size:
  - 512
  - 512
  batch_size: 32
  num_workers: 8
model:
  name: unet
  lr: 0.0001
trainer:
  max_epochs: 50
  accelerator: gpu
  devices: 1
  precision: 16-mixed
  log_every_n_steps: 10
logger:
  project: photochrom-ai
